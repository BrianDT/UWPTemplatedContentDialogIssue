<ContentDialog
    x:Class="TemplatedContentDialogIssue.Shared.Controls.ProgressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TemplatedContentDialogIssue.Shared.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
>

    <ContentDialog.Template>
        <ControlTemplate TargetType="ContentDialog">
            <Border x:Name="outerborder" BorderThickness="0" CornerRadius="8" Background="#AA000000" Padding="8,4,8,4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="This is where messages and and UNDO button would normally be"
                           TextWrapping="WrapWholeWords"
                           Margin="16,0,16,0"
                           VerticalAlignment="Center"
                           Foreground="White" FontSize="12"/>
                    <Button Grid.Column="1" Content="UNDO" Click="Button_Click"
                            Background="Gray"
                            Foreground="White"
                        Margin="12,4,12,4"/>
                </Grid>
            </Border>
        </ControlTemplate>
    </ContentDialog.Template>
</ContentDialog>
